{% extends 'base.html.twig' %}
{% block title %}{{ ship.name }}{% endblock %}
{% block body %}
    <h1>{{ ship.name}}</h1>
    <table>
        <tr>
            <th> Id</th>
            <td>{{ship.id}}</td>
        </tr>
        <tr>
            <th> Name</th>
            <td>{{ship.name|upper}}</td>
        </tr>
    </table>
{% endblock %}